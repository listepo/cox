# cox landing v1 screenshots

Captured from local Hugo build (`npm ci && npm run build:css && hugo --minify` in `website/`).

| File | Theme | Viewport |
|------|-------|----------|
| `home-light.png` | light | 1440×900 desktop |
| `home-dark.png` | dark | 1440×900 desktop |

Theme toggle: system → light → dark (`localStorage` key `cox-theme`).
Brand mark from `docs/brand/logo.svg`.
