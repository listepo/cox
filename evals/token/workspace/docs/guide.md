# Guide

How to work in this repo:

1. Read the file you are about to change first.
2. Keep edits diff-shaped and small.
3. Run the tests before you finish.

Authentication goes through `src/auth.rs`. Widgets live in `src/lib.rs`.
The example in `src/main.rs` shows both working together.
