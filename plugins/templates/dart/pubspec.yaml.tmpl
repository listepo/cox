name: {{crate_name}}
description: >-
  A cox plugin scaffolded by `cox plugin new --lang dart`; an MCP stdio
  server, cox's only Dart capability (PL§13).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  dart_mcp: ^0.5.2

dev_dependencies:
  test: ^1.25.0
