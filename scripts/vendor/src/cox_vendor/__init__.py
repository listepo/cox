"""cox-vendor: the one entry point for a file no package manager fetches
(plan.md A48) — a vendored API spec, a price or model table, any JSON/YAML
data pulled from outside the repo. No hand download, no pasted rows.

See `registry.COMMANDS` for what it can produce today.
"""
