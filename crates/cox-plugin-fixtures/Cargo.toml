[package]
name = "cox-plugin-fixtures"
description = "Built plugin packages for cox's own tests and benches (PL§13)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

# No dependencies: `build.rs` runs the guest workspace's own cargo for
# `wasm32-unknown-unknown` (plugins/, PL§9), so no guest crate ever builds
# for the host here.
