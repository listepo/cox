# T28.4: the model calls `leak`, whose scripted tool output contains a
# secret-shaped string (the stub lives in redact.rs's tests).
[[turn]]
text = "here you go"
tool_calls = [{ name = "leak", input = {} }]

[[turn]]
text = "done"
