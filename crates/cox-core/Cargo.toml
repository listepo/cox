[package]
name = "cox-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
cox-protocol = { path = "../cox-protocol", version = "0.1.0" }
async-trait = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tokio-util = { workspace = true }
# T2.2: permission rules compile path globs (`Read(~/.ssh/**)`) once.
globset = { workspace = true }

[dev-dependencies]
cox-provider = { path = "../cox-provider", version = "0.1.0" }
# T2.5: the `big_tool_output` scenario follows the trailer with a real `expand` call.
cox-tools = { path = "../cox-tools", version = "0.1.0" }
insta = { workspace = true }
serde = { workspace = true }
proptest = { workspace = true }
rstest = { workspace = true }
